# Cloudflare Pages — Git builds use pages_build_output_dir; do NOT set Deploy command
# to `npx wrangler deploy` in the dashboard (that is for Workers, not static Pages).
name = "tcg-tracker"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"
